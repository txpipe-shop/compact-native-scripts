# Examples

This directory contains examples to be used for testing.
In the [`inputs/`](/examples/inputs/) directory there are input files in JSON format. The commitments in each of these are obtained from the [`example-pairs.json`](./example-pairs.json) file contains the secret key and randomness used to obtain each commitment.
